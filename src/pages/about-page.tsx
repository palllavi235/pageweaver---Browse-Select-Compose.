import { ArrowLeft, Github, Heart, Layers3, ShieldCheck, Zap } from 'lucide-react'
import { Link } from 'react-router-dom'
import { Brand } from '@/components/brand'
import { Button, Card } from '@/components/ui'

export function AboutPage() {
  return <div className="min-h-screen bg-paper"><header className="page-shell flex h-20 items-center justify-between"><Brand /><Link to="/app"><Button variant="secondary"><ArrowLeft className="size-4" /> Back to workspace</Button></Link></header><main className="page-shell py-16 sm:py-24"><section className="mx-auto max-w-3xl text-center"><span className="inline-flex rounded-full bg-[#eee3d2] px-3 py-1 text-xs font-bold uppercase tracking-wider text-leather">Our purpose</span><h1 className="mt-6 font-display text-5xl font-semibold tracking-tight sm:text-6xl">Documents should be easier to shape.</h1><p className="mt-7 text-lg leading-8 text-muted">PageWeaver began with a simple frustration: combining a few pages from a handful of PDFs should not require a folder full of temporary files and five different tools.</p></section><section className="mt-16 grid gap-5 md:grid-cols-3">{[{ icon: Zap, title: 'Fast by default', text: 'Every interaction is designed to stay responsive, even as documents grow.' }, { icon: Layers3, title: 'Visual and direct', text: 'See the pages, shape the order, and always understand the result.' }, { icon: ShieldCheck, title: 'Yours by design', text: 'Your documents remain private, portable, and under your control.' }].map(({ icon: Icon, title, text }) => <Card key={title} className="p-7"><Icon className="size-6 text-leather" /><h2 className="mt-6 font-display text-2xl font-semibold">{title}</h2><p className="mt-3 text-sm leading-6 text-muted">{text}</p></Card>)}</section><section className="mx-auto mt-20 max-w-2xl text-center"><Heart className="mx-auto size-6 text-danger" /><h2 className="mt-4 font-display text-3xl font-semibold">Made with care for people who work with ideas.</h2><p className="mt-4 leading-7 text-muted">Students, researchers, teachers, designers, and thoughtful people everywhere deserve tools that respect their time and attention.</p><Button className="mt-7" variant="secondary"><Github className="size-4" /> Follow the journey</Button></section></main></div>
}
